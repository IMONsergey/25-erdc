{
  "name": "25-erdc",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "prepare:assets": "node scripts/prepare-portal.mjs && node scripts/prepare-regional-atlas.mjs && node scripts/prepare-public.mjs",
    "predev": "npm run prepare:assets",
    "dev": "vite",
    "prebuild": "npm run prepare:assets",
    "build": "node scripts/check-render.mjs && vite build && node scripts/build-routes.mjs && node scripts/verify-portal.mjs",
    "preview": "vite preview",
    "check:render": "node scripts/check-render.mjs"
  },
  "dependencies": {
    "lucide": "1.47.0",
    "morphicons": "1.7.1",
    "ol": "^10.10.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.5",
    "vite": "8.2.1"
  }
}
